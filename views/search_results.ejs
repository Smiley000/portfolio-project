<!DOCTYPE html>
<html>
<head><title>Search Results - Pet Pals</title></head>
<body>
<h2>Search Results</h2>
<% if (results.length > 0) { %>
  <ul>
  <% results.forEach(pet => { %>
    <li><strong><%= pet.name %></strong> (<%= pet.type %>) - <%= pet.description %></li>
  <% }) %>
  </ul>
<% } else { %>
  <p>No pets found.</p>
<% } %>
<a href="/">Home</a>
</body>
</html>
